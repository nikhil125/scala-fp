package chapter3

/**
	* Implement tail function for removing the first element of a List. Note that the function takes constant time.
	* What are different choices you could make in your implementation if the List is Nil?
	*/
object Exercise_3_2 {
 def main(as:Array[String]) :Unit = {
//	 println(List(2,3,4))
	 val s: Stream[Int] = Stream(1,2,3,4,5)
 }
}
